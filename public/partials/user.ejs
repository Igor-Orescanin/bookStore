


    <button onclick="del(event)">Delete</button>
    <div class="d-flex flex-column align-items-center">
            <div class="card" style="width: 18rem; height:18rem;">
                <img class="card-img-top" src="https://source.unsplash.com/random" alt="Book cover image">
                <div class="card-body">
                  <h2 class="card-title">User : <%= data.fName %> <%= data.lName %></h2>
                  <h4 class="card-text">Email: <%= data.email %></h4>
                  <p class="card-text">Password: <%= data.password %></p>
                  <p class="card-text">City: <%= data.address[0].city %></p>
                </div>
              </div>
            <div class="d-flex justify-content-center">
                <form action="/users/updateUser/<%= data._id%>" method="GET">
                    <button class="btn btn-success" >Update</button>
                </form>
                <button class="btn btn-danger" onclick="del(event)">Delete</button>
                <form action="/users" method="GET">
                    <button class="btn btn-warning">Back to Users</button>
                </form>
            </div>
        </div>



    <script>
function del(e){
    e.preventDefault()
    let url = "/users/<%=data._id%>"
    console.log(url)
    fetch(url, {
        method: 'delete',
        headers: {'Content-Type': 'application/json'}
    }).then(data => {
        data.json().then( m => {
            if(m.status){
            res.json({
                message: 'You got deleteeeeed'
            })
            }else{
                console.log("Failed")
            }
        })
    })
}
    </script> 
